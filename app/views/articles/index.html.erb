<%= form_with model: [@project, @article] do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.submit "Create article" %>
<% end %>

<% @project.articles.each do |article| %>
  <%= article.title %>
<% end %>
